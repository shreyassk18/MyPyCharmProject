#If we want to know what all classes present ikn the module we need to use dir command

import x
print(dir(x))

#If we want to know what all functions are present in the module
import y
print(dir(y))

